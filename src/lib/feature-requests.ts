// Feature/question requests submitted by users from the settings page,
// persisted in the `feature_requests` table (see supabase/migrations).

export type FeatureRequestType = "feature" | "question";
